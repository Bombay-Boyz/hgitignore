# benchmark

Intentionally empty until Phase 14 (Benchmarking).

`gauge`-based benchmarks for `compile` and `matches`/`matchesMany` will be
added here once Phase 12 (Git Parity) is fully checked off, per the Guiding
Principle in `SPEC.md`: no benchmark-driven optimization work is permitted
before conformance is established.
